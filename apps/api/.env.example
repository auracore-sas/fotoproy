# Copia este archivo a `.env` y ajusta los valores (el .env NO se versiona).
NODE_ENV=development
PORT=4100
DATABASE_URL="postgresql://fotoproy:fotoproy@localhost:55432/fotoproy?schema=public"
# Orígenes permitidos por CORS (separados por coma). '*' permite todos.
CORS_ORIGIN=*
