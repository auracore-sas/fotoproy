# FotoProy API — Dokploy deployment (Compose application).
#
# Why a Compose application instead of an "Application" (Dockerfile) entry:
# in Dokploy, databases and Compose services live on `dokploy-network`, while a
# Compose deployment creates its own isolated network by default. Declaring
# `dokploy-network` as external puts this container on the same network as the
# existing MinIO and PostgreSQL, so the internal hostnames resolve.
#
# Usage (Dokploy → project → Create Compose):
#   1. Provider: Git (or GitHub App) → repository auracore-sas/fotoproy, branch main.
#   2. Compose Path: ./docker-compose.dokploy.yml
#   3. Environment: add the variables listed in docs/deployment.md §3.2. Dokploy
#      writes them into a .env that `docker compose --env-file` only uses to
#      interpolate the ${...} placeholders below; it does not inject them into
#      the container by itself, which is why every value the API needs is
#      mapped explicitly under `environment:`.
#   4. Do NOT add a Domain in the Dokploy UI: the Traefik labels below already
#      route fotoproy.apx5.com to this container (same style as the MinIO stack).
#
# The image is built from the repository Dockerfile; migrations run inside the
# container on start when RUN_MIGRATIONS_ON_START=true.

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: fotoproy-api:latest
    container_name: fotoproy-api
    restart: always
    # The data plane uses the internal MinIO host; pre-signed URLs are signed
    # against STORAGE_PUBLIC_ENDPOINT (see docs/deployment.md §3.3).
    environment:
      NODE_ENV: production
      PORT: '4100'
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL}
      CORS_ORIGIN: ${CORS_ORIGIN}
      # Internal MinIO endpoint, fixed here on purpose. The alias must NOT
      # contain underscores: MinIO validates the Host header and answers
      # "400 InvalidRequest (invalid hostname)" for names like `minio_storage`,
      # even though Docker resolves them. Keeping it in this file (instead of
      # the Dokploy Environment tab, whose .env is only used for ${...}
      # interpolation) makes the deploy deterministic.
      #
      # If the Docker alias cannot be used, set the public S3 endpoint instead
      # (works through Traefik; only the server-side reads/writes of photos and
      # thumbnails take the extra hop, never the phone uploads):
      #   STORAGE_ENDPOINT: https://minio-api.apx5.com
      STORAGE_ENDPOINT: http://minio:9000
      STORAGE_PUBLIC_ENDPOINT: ${STORAGE_PUBLIC_ENDPOINT}
      STORAGE_REGION: ${STORAGE_REGION:-us-east-1}
      STORAGE_ACCESS_KEY_ID: ${STORAGE_ACCESS_KEY_ID}
      STORAGE_SECRET_ACCESS_KEY: ${STORAGE_SECRET_ACCESS_KEY}
      STORAGE_BUCKET: ${STORAGE_BUCKET:-fotoproy}
      STORAGE_FORCE_PATH_STYLE: ${STORAGE_FORCE_PATH_STYLE:-true}
      STORAGE_SIGNED_URL_TTL: ${STORAGE_SIGNED_URL_TTL:-3600}
      STORAGE_GENERATE_THUMBS: ${STORAGE_GENERATE_THUMBS:-true}
      STORAGE_STAMP_PHOTOS: ${STORAGE_STAMP_PHOTOS:-true}
      STORAGE_STRIP_EXIF: ${STORAGE_STRIP_EXIF:-true}
      # F4.5 — upload caps (MB) and rate limiting. Defaults come from the shared
      # contract; override them in the Dokploy Environment tab if needed.
      UPLOAD_MAX_PHOTO_MB: ${UPLOAD_MAX_PHOTO_MB:-25}
      UPLOAD_MAX_VIDEO_MB: ${UPLOAD_MAX_VIDEO_MB:-200}
      UPLOAD_MAX_PLAN_MB: ${UPLOAD_MAX_PLAN_MB:-40}
      RATE_LIMIT_ENABLED: ${RATE_LIMIT_ENABLED:-true}
      RATE_LIMIT_WINDOW_SECONDS: ${RATE_LIMIT_WINDOW_SECONDS:-60}
      RATE_LIMIT_MAX: ${RATE_LIMIT_MAX:-600}
      RATE_LIMIT_PUBLIC_MAX: ${RATE_LIMIT_PUBLIC_MAX:-120}
      RATE_LIMIT_MEDIA_MAX: ${RATE_LIMIT_MEDIA_MAX:-1200}
      # Cloudflare is in front of both domains here, so the real visitor comes in
      # `CF-Connecting-IP`. Set to false if the origin ever becomes reachable
      # directly (otherwise the header could be spoofed to dodge the limit).
      RATE_LIMIT_TRUST_CF_HEADER: ${RATE_LIMIT_TRUST_CF_HEADER:-true}
      TRUST_PROXY: ${TRUST_PROXY:-false}
      JSON_BODY_LIMIT: ${JSON_BODY_LIMIT:-1mb}
      RUN_MIGRATIONS_ON_START: ${RUN_MIGRATIONS_ON_START:-true}
    networks:
      - dokploy-network
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.fotoproy-api.rule=Host(`fotoproy.apx5.com`)'
      - 'traefik.http.routers.fotoproy-api.entrypoints=websecure'
      - 'traefik.http.routers.fotoproy-api.tls.certresolver=letsencrypt'
      - 'traefik.http.services.fotoproy-api.loadbalancer.server.port=4100'

networks:
  dokploy-network:
    external: true
